<resources>

    <!-- Theme for activities that don't need action bar -->
    <style name="NoActionBarTheme" parent="android:Theme.Holo.Light.NoActionBar.Fullscreen">

        <!-- These statements are required in order to prevent actionbar from being seen
         before splash screen (there is additional logic in activity that supports this feature) -->
        <!--<item name="android:windowActionBar">false</item>-->
        <!--<item name="android:windowTitleSize">0dp</item>-->

        <!--<item name="android:actionBarStyle">@style/MyActionBar</item>-->

    </style>


    <!-- Theme for activities that don't need action bar -->
    <style name="DefaultTheme" parent="android:Theme.Holo.Light.DarkActionBar">

        <item name="android:actionBarStyle">@style/MyActionBar</item>

        <!-- These statements are required in order to prevent actionbar from being seen
         before splash screen (there is additional logic in activity that supports this feature) -->
        <!--<item name="android:windowActionBar">false</item>-->
        <!--<item name="android:windowTitleSize">0dp</item>-->
    </style>


    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@drawable/actionbar_background</item>
    </style>


</resources>
